function p() {
  const c = /^.*\/(?<componentDir>components)\/(?<componentName>[^/]+)\/index\.ts$/;
  return {
    name: "salla-component-transform",
    enforce: "pre",
    transform(t, n) {
      var r;
      const o = n.match(c);
      if (!o)
        return null;
      const e = t.match(/class\s+(\w+)/i);
      if (!e)
        return null;
      const s = e[1], a = ((r = o.groups) == null ? void 0 : r.componentName) || n.split("/")[n.split("/").indexOf("components") + 1], m = a.substring(0, 6).toLowerCase() === "salla-" ? "" : "salla-";
      return { code: `${t}
        if (typeof ${s} !== 'undefined') {${s}.registerSallaComponent('${m + a}');}` };
    }
  };
}
export {
  p as sallaTransformPlugin
};
